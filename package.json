{
  "scripts": {
    "dev": "bun run --hot src/index.tsx",
    "build": "bun run tailwind && bun build src/index.tsx --minify --target=node --outfile=dist/index.js",
    "check": "bunx @biomejs/biome check --apply src/**",
    "check:unsafe": "bunx @biomejs/biome check --apply-unsafe src/**"
  },
  "dependencies": {
    "hono": "^4.2.6",
    "postgres": "^3.4.4",
    "redis": "^4.6.11"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.7.0",
    "bun-types": "^1.0.7"
  },
  "module": "esnext"
}
